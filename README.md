# this is Grad Linux™

welcome to Grad Linux™, my very own distro, featuring:

- my own fork of fluxbox named Grad Fluxbox™, because that's just what i'd do :trollface:
- the `musl` libc, because `glibc` should go to hell imo
- `busybox`, because i can't be bothered to build `coreutils`
- the `tcc` compiler, because `gcc` is too popular
- my very own, state of the art, `scavenge` compiler, with never seen before syntax to install a thing or two :eyes:
- `aria2c`, because busybox's `wget` doesn't check certificates and isn't multithreaded
- `bootc`, because backing up for a distro hop is idiotic

i can think of a bajillion mottos, here are some i'd like to share though:

1. "Maintained on a crappy laptop, for all the world's crappy laptops."
2. "First we Grad, then we Linux. Let's Grad Linux™!"
3. "Alpine Linux, but with `bootc` and `tcc`."
4. "The Linux distro maintained by the totally popular Gradient Man."
5. "The world's 5th-or-so Linux distro to feature Fluxbox out of the box."
6. "The world's first Linux distro to feature Grad Fluxbox™."

my favorite is the first one though, so i guess i'll use that as the main motto

## have the checklist i am basing development off of

- [x] kernel
- [x] musl
- [x] busybox
- [x] tcc
- [x] scavenge
- [ ] aria2c ←
- [ ] (gnu) bash
- [ ] bootc?
- [ ] x11
- [ ] fluxbox fork (grad fluxbox)
- [ ] gtk3?
- [ ] atleast firefox

## build

idfk follow the github action you psycho
